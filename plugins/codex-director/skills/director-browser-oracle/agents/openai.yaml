interface:
  display_name: "Director Browser Oracle"
  short_description: "Run a delegated Browser oracle lane"
  default_prompt: "Use $codex-director:director-browser-oracle to run a delegated Browser oracle review with privacy checks and concise evidence."

policy:
  allow_implicit_invocation: false
