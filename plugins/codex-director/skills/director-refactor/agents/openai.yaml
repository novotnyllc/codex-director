interface:
  display_name: "Director Refactor"
  short_description: "Run a behavior-preserving refactor lane"
  default_prompt: "Use $codex-director:director-refactor to complete behavior-preserving cleanup with verification and concise evidence."

policy:
  allow_implicit_invocation: false
