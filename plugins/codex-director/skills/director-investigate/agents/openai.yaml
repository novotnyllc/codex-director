interface:
  display_name: "Director Investigate"
  short_description: "Run a read-only Director research lane"
  default_prompt: "Use $codex-director:director-investigate to gather read-only evidence and report findings, confidence, and next actions."

policy:
  allow_implicit_invocation: false
