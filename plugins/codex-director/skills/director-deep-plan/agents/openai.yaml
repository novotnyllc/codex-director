interface:
  display_name: "Director Deep Plan"
  short_description: "Produce a reviewed Director plan"
  default_prompt: "Use $codex-director:director-deep-plan to produce a reviewed implementation or architecture plan without editing code."

policy:
  allow_implicit_invocation: false
