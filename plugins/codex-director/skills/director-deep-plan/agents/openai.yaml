interface:
  display_name: "Director Deep Plan"
  short_description: "Produce a reviewed Director plan"
  default_prompt: "Use $director-deep-plan to create a reviewed implementation or architecture plan for this Director worker assignment without editing code, reporting any native helper runtime and verified helper evidence."

policy:
  allow_implicit_invocation: false
