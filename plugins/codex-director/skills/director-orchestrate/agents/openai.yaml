interface:
  display_name: "Director Orchestrate"
  short_description: "Coordinate a bounded Director worker lane"
  default_prompt: "Use $director-orchestrate to coordinate this Director worker assignment with helper lanes, sequencing, evidence reconciliation, and concise final status."

policy:
  allow_implicit_invocation: false
