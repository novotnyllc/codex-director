interface:
  display_name: "Director Optimize"
  short_description: "Run a measurement-led optimization lane"
  default_prompt: "Use $director-optimize to measure, improve, and verify this Director worker performance or efficiency assignment."

policy:
  allow_implicit_invocation: false
