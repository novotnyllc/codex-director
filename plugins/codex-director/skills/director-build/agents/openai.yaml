interface:
  display_name: "Director Build"
  short_description: "Run a bounded Director implementation lane"
  default_prompt: "Use $codex-director:director-build for bounded implementation with edits, verification, review, and evidence."

policy:
  allow_implicit_invocation: false
