# Repository Instructions

- When updating the Codex Director plugin version in `plugins/codex-director/.codex-plugin/plugin.json`, also update the matching `codex-director` entry in `/Users/claire/dev/marketplace/.agents/plugins/plugin-versions.json`, then commit and push both repositories.
